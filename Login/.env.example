# .env.example — copy this to .env and fill in real values
# .env itself is gitignored and must NEVER be committed.

DB_HOST=mysql-1c962090-cos20031-group4.b.aivencloud.com
DB_PORT=30302
DB_NAME=SmartFleetDatabase
DB_USER=avnadmin
DB_PASS=AVN_2KSEO@k$k3